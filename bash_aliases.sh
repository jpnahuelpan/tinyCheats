#!/bin/bash

# bash commands
alias nf="touch"  # new file => nf
alias vf="cat"    # view file => vf
alias cdp="cd .." # cd parent => cdp

# bash files
alias ghtokens="vf $HOME/.tokens_file" # view yours tokens in tokens file.

#  git commands 
alias gits="git status"
alias gitl="git log"
alias gitc="git commit -m"
alias gitpush = "git push -u origin main"
